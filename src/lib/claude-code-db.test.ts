import { afterEach, describe, expect, it } from 'bun:test';
import { randomUUID } from 'node:crypto';
import { mkdir, mkdtemp, rm } from 'node:fs/promises';
import os from 'node:os';
import path from 'node:path';
import {
    deleteClaudeCodeSession,
    findClaudeCodeWorkspaceGroups,
    getDefaultClaudeCodeDataDir,
    listClaudeCodeSessionsForGroup,
    listClaudeCodeWorkspaceGroups,
    readClaudeCodeSessionTranscript,
} from './claude-code-db';
import { renderClaudeCodeTranscript } from './claude-code-transcript';
import { getClaudeCodeAssistantMessagePhase } from './claude-code-transcript-phase';

const tempRoots: string[] = [];
const homeDir = os.homedir();
const corpusCwd = path.join(homeDir, 'workspace', 'ushman-corpus');
const otherCwd = path.join(homeDir, 'workspace', 'other');

const makeTempRoot = async () => {
    const tempRoot = await mkdtemp(path.join(os.tmpdir(), 'claude-code-db-test-'));
    tempRoots.push(tempRoot);
    return tempRoot;
};

const writeJsonl = async (filePath: string, records: unknown[]) => {
    await Bun.write(filePath, `${records.map((record) => JSON.stringify(record)).join('\n')}\n`);
};

const writeSession = async (projectsDir: string, projectDirName: string, sessionId: string, records: unknown[]) => {
    const projectDir = path.join(projectsDir, projectDirName);
    await mkdir(projectDir, { recursive: true });
    await writeJsonl(path.join(projectDir, `${sessionId}.jsonl`), records);
};

const buildSessionRecords = (sessionId: string, cwd: string, firstTimestamp = '2026-06-01T10:00:00.000Z') => [
    {
        cwd,
        message: {
            content: 'Descope-Class Vendor-Detection review please',
            role: 'user',
        },
        sessionId,
        timestamp: firstTimestamp,
        type: 'user',
        uuid: `${sessionId}-user-1`,
    },
    {
        cwd,
        message: {
            content: [
                { thinking: 'Need inspect the vendor detection path.', type: 'thinking' },
                {
                    id: 'toolu_1',
                    input: { command: 'rg "Descope-Class Vendor-Detection" .' },
                    name: 'Bash',
                    type: 'tool_use',
                },
                { text: 'I found the vendor detection thread.', type: 'text' },
            ],
            model: 'claude-sonnet-4-5',
            role: 'assistant',
            usage: {
                cache_creation_input_tokens: 2,
                cache_read_input_tokens: 3,
                input_tokens: 10,
                output_tokens: 4,
            },
        },
        parentUuid: `${sessionId}-user-1`,
        sessionId,
        timestamp: '2026-06-01T10:00:04.000Z',
        type: 'assistant',
        uuid: `${sessionId}-assistant-1`,
        version: '2.1.148',
    },
    {
        cwd,
        message: {
            content: [
                {
                    content: 'src/vendor.ts: Descope-Class Vendor-Detection',
                    is_error: false,
                    tool_use_id: 'toolu_1',
                    type: 'tool_result',
                },
            ],
            role: 'user',
        },
        parentUuid: `${sessionId}-assistant-1`,
        sessionId,
        timestamp: '2026-06-01T10:00:05.000Z',
        type: 'user',
        uuid: `${sessionId}-user-2`,
    },
];

const buildMessageRecord = (
    sessionId: string,
    uuid: string,
    role: 'assistant' | 'user',
    content: string,
    timestamp: string,
) => ({
    cwd: corpusCwd,
    message: {
        content,
        id: role === 'assistant' ? `message-${uuid}` : undefined,
        role,
    },
    sessionId,
    timestamp,
    type: role,
    uuid,
});

const buildCompactionRecords = (sessionId: string) => [
    {
        cwd: corpusCwd,
        sessionId,
        timestamp: '2026-06-01T10:02:00.001Z',
        type: 'system',
        uuid: 'compact-boundary',
    },
    {
        cwd: corpusCwd,
        isCompactSummary: true,
        message: {
            content:
                'This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion.',
            role: 'user',
        },
        parentUuid: 'compact-boundary',
        sessionId,
        timestamp: '2026-06-01T10:02:00.000Z',
        type: 'user',
        uuid: 'compact-summary',
    },
    {
        cwd: corpusCwd,
        isMeta: true,
        message: {
            content:
                '<local-command-caveat>Caveat: The messages below were generated by the user while running local commands.</local-command-caveat>',
            role: 'user',
        },
        sessionId,
        timestamp: '2026-06-01T10:02:00.100Z',
        type: 'user',
        uuid: 'compact-caveat',
    },
    {
        cwd: corpusCwd,
        message: {
            content: '<command-name>/compact</command-name>\n<command-message>compact</command-message>',
            role: 'user',
        },
        sessionId,
        timestamp: '2026-06-01T10:02:00.200Z',
        type: 'user',
        uuid: 'compact-command',
    },
    {
        cwd: corpusCwd,
        message: {
            content: '<local-command-stdout>Compacted</local-command-stdout>',
            role: 'user',
        },
        sessionId,
        timestamp: '2026-06-01T10:02:00.300Z',
        type: 'user',
        uuid: 'compact-output',
    },
];

describe('claude code workspace discovery', () => {
    afterEach(async () => {
        await Promise.all(tempRoots.splice(0).map((root) => rm(root, { force: true, recursive: true })));
    });

    it('should resolve the default Claude Code data directory', () => {
        expect(getDefaultClaudeCodeDataDir({}, '/Users/example')).toBe('/Users/example/.claude');
    });

    it('should list workspace groups from local Claude Code project JSONL files', async () => {
        const projectsDir = await makeTempRoot();
        await writeSession(
            projectsDir,
            '-Users-rhaq-workspace-ushman-corpus',
            'session-a',
            buildSessionRecords('session-a', corpusCwd),
        );
        await writeSession(
            projectsDir,
            '-Users-rhaq-workspace-ushman-corpus',
            'session-b',
            buildSessionRecords('session-b', corpusCwd, '2026-06-02T10:00:00.000Z'),
        );
        await writeSession(
            projectsDir,
            '-Users-rhaq-workspace-other',
            'session-c',
            buildSessionRecords('session-c', otherCwd, '2026-05-30T10:00:00.000Z'),
        );

        const workspaces = await listClaudeCodeWorkspaceGroups(projectsDir);

        expect(workspaces).toHaveLength(2);
        expect(workspaces[0]).toMatchObject({
            label: 'ushman-corpus',
            messageCount: 4,
            sessionCount: 2,
            toolCallCount: 2,
            worktree: corpusCwd,
        });
        expect(findClaudeCodeWorkspaceGroups(workspaces, '~/workspace/ushman-corpus')).toHaveLength(1);
        expect(findClaudeCodeWorkspaceGroups(workspaces, 'other')[0]?.worktree).toBe(otherCwd);
    });

    it('should skip null JSONL records without losing valid Claude Code messages', async () => {
        const projectsDir = await makeTempRoot();
        await writeSession(projectsDir, '-Users-rhaq-workspace-ushman-corpus', 'session-null', [
            null,
            ...buildSessionRecords('session-null', corpusCwd),
        ]);

        const originalWarn = console.warn;
        console.warn = () => undefined;
        try {
            const transcript = await readClaudeCodeSessionTranscript(projectsDir, 'session-null');

            expect(transcript?.session.messageCount).toBe(2);
            expect(transcript?.entries).toHaveLength(3);
        } finally {
            console.warn = originalWarn;
        }
    });

    it('should list sessions for a workspace and parse a selected transcript', async () => {
        const projectsDir = await makeTempRoot();
        await writeSession(
            projectsDir,
            '-Users-rhaq-workspace-ushman-corpus',
            'session-a',
            buildSessionRecords('session-a', corpusCwd),
        );

        const workspaces = await listClaudeCodeWorkspaceGroups(projectsDir);
        const sessions = await listClaudeCodeSessionsForGroup(workspaces[0]?.key ?? '', projectsDir);
        const transcript = await readClaudeCodeSessionTranscript(projectsDir, 'session-a');

        expect(sessions).toHaveLength(1);
        expect(sessions[0]).toMatchObject({
            assistantMessageCount: 1,
            messageCount: 2,
            model: 'claude-sonnet-4-5',
            sessionId: 'session-a',
            title: 'Descope-Class Vendor-Detection review please',
            toolCallCount: 1,
            toolResultCount: 1,
            totalTokens: 19,
            userMessageCount: 1,
        });
        expect(transcript?.session.sessionId).toBe('session-a');
        expect(transcript?.entries).toHaveLength(3);
        expect(transcript?.entries[0]?.parts[0]).toMatchObject({
            text: 'Descope-Class Vendor-Detection review please',
            type: 'text',
        });
        expect(transcript?.entries[1]?.parts.map((part) => part.type)).toEqual(['thinking', 'tool_use', 'text']);
        expect(transcript?.entries[1]?.parts[1]).toMatchObject({
            argumentsText: '{\n  "command": "rg \\"Descope-Class Vendor-Detection\\" ."\n}',
            toolName: 'Bash',
            toolUseId: 'toolu_1',
        });
        expect(transcript?.entries[2]?.parts[0]).toMatchObject({
            outputText: 'src/vendor.ts: Descope-Class Vendor-Detection',
            toolUseId: 'toolu_1',
            type: 'tool_result',
        });
        expect(transcript?.rawEvents).toHaveLength(3);
    });

    it('should count Claude response fragments once and exclude synthetic user rows from message totals', async () => {
        const projectsDir = await makeTempRoot();
        const sessionId = 'session-fragments';
        await writeSession(projectsDir, '-Users-rhaq-workspace-ushman-corpus', sessionId, [
            {
                cwd: corpusCwd,
                isCompactSummary: true,
                message: { content: 'Synthetic compaction summary', role: 'user' },
                sessionId,
                timestamp: '2026-06-01T10:00:00.000Z',
                type: 'user',
                uuid: 'compact-summary',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: '<command-name>/compact</command-name>\n<command-message>compact</command-message>',
                    role: 'user',
                },
                sessionId,
                timestamp: '2026-06-01T10:00:00.500Z',
                type: 'user',
                uuid: 'compact-command',
            },
            {
                cwd: corpusCwd,
                message: {
                    content:
                        '<!-- attach -->\n> Previous assistant answer\n> More quoted context\n\nPlease inspect this',
                    role: 'user',
                },
                sessionId,
                timestamp: '2026-06-01T10:00:01.000Z',
                type: 'user',
                uuid: 'user-prompt',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: [{ thinking: 'Inspect first', type: 'thinking' }],
                    id: 'message-a',
                    model: 'claude-opus-4-8',
                    role: 'assistant',
                    usage: { input_tokens: 10, output_tokens: 20 },
                },
                sessionId,
                timestamp: '2026-06-01T10:00:02.000Z',
                type: 'assistant',
                uuid: 'assistant-thinking',
                version: '2.1.148',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: [{ id: 'tool-a', input: {}, name: 'Read', type: 'tool_use' }],
                    id: 'message-a',
                    model: 'claude-opus-4-8',
                    role: 'assistant',
                    usage: { input_tokens: 10, output_tokens: 20 },
                },
                sessionId,
                timestamp: '2026-06-01T10:00:03.000Z',
                type: 'assistant',
                uuid: 'assistant-tool',
                version: '2.1.148',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: [{ text: 'Inspection complete', type: 'text' }],
                    id: 'message-a',
                    model: 'claude-opus-4-8',
                    role: 'assistant',
                    usage: { input_tokens: 10, output_tokens: 20 },
                },
                sessionId,
                timestamp: '2026-06-01T10:00:04.000Z',
                type: 'assistant',
                uuid: 'assistant-text',
                version: '2.1.148',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: [{ content: 'tool output', tool_use_id: 'tool-a', type: 'tool_result' }],
                    role: 'user',
                },
                sessionId,
                timestamp: '2026-06-01T10:00:05.000Z',
                type: 'user',
                uuid: 'tool-result',
            },
        ]);

        const sessions = await listClaudeCodeSessionsForGroup(
            'project:-Users-rhaq-workspace-ushman-corpus',
            projectsDir,
        );

        expect(sessions[0]).toMatchObject({
            assistantMessageCount: 1,
            messageCount: 2,
            title: 'Please inspect this',
            toolCallCount: 1,
            toolResultCount: 1,
            totalTokens: 30,
            userMessageCount: 1,
        });
    });

    it('should coalesce compacted continuations into one full logical session', async () => {
        const projectsDir = await makeTempRoot();
        const projectDirName = '-Users-rhaq-workspace-ushman-corpus';
        await writeSession(projectsDir, projectDirName, 'session-root', [
            buildMessageRecord(
                'session-root',
                'root-user',
                'user',
                'Original user request',
                '2026-06-01T10:00:00.000Z',
            ),
            buildMessageRecord(
                'session-root',
                'root-assistant',
                'assistant',
                'Original assistant response',
                '2026-06-01T10:01:00.000Z',
            ),
            ...buildCompactionRecords('session-root'),
            buildMessageRecord(
                'session-root',
                'root-branch-user',
                'user',
                'Abandoned root branch',
                '2026-06-01T10:03:00.000Z',
            ),
        ]);
        await writeSession(projectsDir, projectDirName, 'session-abandoned', [
            ...buildCompactionRecords('session-abandoned'),
            buildMessageRecord(
                'session-abandoned',
                'abandoned-user',
                'user',
                'Abandoned continuation branch',
                '2026-06-01T10:30:00.000Z',
            ),
        ]);
        await writeSession(projectsDir, projectDirName, 'session-active', [
            ...buildCompactionRecords('session-active'),
            buildMessageRecord(
                'session-active',
                'active-user',
                'user',
                'Continued user request',
                '2026-06-01T11:00:00.000Z',
            ),
            buildMessageRecord(
                'session-active',
                'active-assistant',
                'assistant',
                'Continued assistant response',
                '2026-06-01T11:01:00.000Z',
            ),
        ]);

        const workspaces = await listClaudeCodeWorkspaceGroups(projectsDir);
        const physicalSessions = await listClaudeCodeSessionsForGroup(
            'project:-Users-rhaq-workspace-ushman-corpus',
            projectsDir,
        );
        const sessions = await listClaudeCodeSessionsForGroup(
            'project:-Users-rhaq-workspace-ushman-corpus',
            projectsDir,
            { merged: true },
        );
        const physicalTranscript = await readClaudeCodeSessionTranscript(projectsDir, 'session-abandoned');
        const transcript = await readClaudeCodeSessionTranscript(projectsDir, 'session-active', { merged: true });
        const aliasTranscript = await readClaudeCodeSessionTranscript(projectsDir, 'session-abandoned', {
            merged: true,
        });
        const exported = transcript
            ? renderClaudeCodeTranscript(transcript, {
                  includeCommentary: true,
                  includeMetadata: true,
                  includeTools: true,
                  outputFormat: 'md',
              })
            : null;

        expect(workspaces[0]).toMatchObject({ sessionCount: 3 });
        expect(physicalSessions).toHaveLength(3);
        expect(physicalTranscript?.session.sessionId).toBe('session-abandoned');
        expect(
            physicalTranscript?.entries.flatMap((entry) => entry.parts.map((part) => part.text)).filter(Boolean),
        ).toContain('Abandoned continuation branch');
        expect(sessions).toHaveLength(1);
        expect(sessions[0]).toMatchObject({
            assistantMessageCount: 2,
            messageCount: 4,
            sessionId: 'session-active',
            title: 'Original user request',
            userMessageCount: 2,
        });
        expect(transcript?.entries.flatMap((entry) => entry.parts.map((part) => part.text)).filter(Boolean)).toEqual([
            'Original user request',
            'Original assistant response',
            'Continued user request',
            'Continued assistant response',
        ]);
        expect(transcript?.rawEvents.some((event) => event.isCompactSummary === true)).toBe(true);
        expect(aliasTranscript?.session.sessionId).toBe('session-active');
        expect(aliasTranscript?.entries.map((entry) => entry.entryId)).toEqual(
            transcript?.entries.map((entry) => entry.entryId),
        );
        expect(exported).toContain('Original user request');
        expect(exported).toContain('Original assistant response');
        expect(exported).toContain('Continued user request');
        expect(exported).toContain('Continued assistant response');
        expect(exported).not.toContain('This session is being continued');
        expect(exported).not.toContain('<command-name>/compact</command-name>');
        expect(exported).not.toContain('Abandoned root branch');
        expect(exported).not.toContain('Abandoned continuation branch');
    });

    it('should delete every physical transcript in a compacted session lineage', async () => {
        const projectsDir = await makeTempRoot();
        const projectDirName = '-Users-rhaq-workspace-ushman-corpus';
        const sessionIds = ['session-root', 'session-active'];
        await writeSession(projectsDir, projectDirName, 'session-root', [
            buildMessageRecord(
                'session-root',
                'root-user',
                'user',
                'Original user request',
                '2026-06-01T10:00:00.000Z',
            ),
            ...buildCompactionRecords('session-root'),
        ]);
        await writeSession(projectsDir, projectDirName, 'session-active', [
            ...buildCompactionRecords('session-active'),
            buildMessageRecord(
                'session-active',
                'active-user',
                'user',
                'Continued user request',
                '2026-06-01T11:00:00.000Z',
            ),
        ]);

        const result = await deleteClaudeCodeSession(projectsDir, 'session-active', { merged: true });

        expect(result.deletedSessionIds.sort()).toEqual([...sessionIds].sort());
        for (const sessionId of sessionIds) {
            expect(await Bun.file(path.join(projectsDir, projectDirName, `${sessionId}.jsonl`)).exists()).toBe(false);
        }
    });

    it('should tolerate concurrent deletes for aliases in the same compacted lineage', async () => {
        const projectsDir = await makeTempRoot();
        const projectDirName = '-Users-rhaq-workspace-ushman-corpus';
        await writeSession(projectsDir, projectDirName, 'session-root', [
            buildMessageRecord(
                'session-root',
                'root-user',
                'user',
                'Original user request',
                '2026-06-01T10:00:00.000Z',
            ),
            ...buildCompactionRecords('session-root'),
        ]);
        await writeSession(projectsDir, projectDirName, 'session-active', [
            ...buildCompactionRecords('session-active'),
            buildMessageRecord(
                'session-active',
                'active-user',
                'user',
                'Continued user request',
                '2026-06-01T11:00:00.000Z',
            ),
        ]);

        await expect(
            Promise.all([
                deleteClaudeCodeSession(projectsDir, 'session-root'),
                deleteClaudeCodeSession(projectsDir, 'session-active'),
            ]),
        ).resolves.toBeArray();
        expect(await listClaudeCodeSessionsForGroup(`project:${projectDirName}`, projectsDir)).toEqual([]);
    });

    it('should omit metadata-only Claude JSONL files from workspace and session lists', async () => {
        const projectsDir = await makeTempRoot();
        await writeSession(projectsDir, '-Users-rhaq-workspace-ushman-corpus', 'metadata-only', [
            { sessionId: 'metadata-only', title: 'Unused draft', type: 'custom-title' },
            { mode: 'default', sessionId: 'metadata-only', type: 'mode' },
            {
                message: {
                    content: '<command-name>/compact</command-name>\n<command-message>compact</command-message>',
                    role: 'user',
                },
                sessionId: 'metadata-only',
                type: 'user',
                uuid: 'compact-command',
            },
        ]);

        expect(await listClaudeCodeWorkspaceGroups(projectsDir)).toEqual([]);
        expect(
            await listClaudeCodeSessionsForGroup('project:-Users-rhaq-workspace-ushman-corpus', projectsDir),
        ).toEqual([]);
        expect(await readClaudeCodeSessionTranscript(projectsDir, 'metadata-only')).not.toBeNull();
    });

    it('should prefer a sibling transcript cwd over lossy hyphenated directory decoding', async () => {
        const projectsDir = await makeTempRoot();
        const directoryName = '-Users-rhaq-workspace-my-project';
        const expectedWorktree = path.join(homeDir, 'workspace', 'my-project');
        await writeSession(projectsDir, directoryName, 'a-no-cwd', [
            {
                message: { content: 'No cwd in this record', role: 'user' },
                sessionId: 'a-no-cwd',
                type: 'user',
                uuid: 'a-no-cwd-user',
            },
        ]);
        await writeSession(projectsDir, directoryName, 'b-with-cwd', [
            {
                cwd: expectedWorktree,
                message: { content: 'Canonical cwd', role: 'user' },
                sessionId: 'b-with-cwd',
                type: 'user',
                uuid: 'b-with-cwd-user',
            },
        ]);

        const groups = await listClaudeCodeWorkspaceGroups(projectsDir);

        expect(groups[0]?.worktree).toBe(expectedWorktree);
        expect(groups[0]?.label).toBe('my-project');
    });

    it('should omit raw payloads when requested for large UI responses', async () => {
        const projectsDir = await makeTempRoot();
        await writeSession(
            projectsDir,
            '-Users-rhaq-workspace-ushman-corpus',
            'session-a',
            buildSessionRecords('session-a', corpusCwd),
        );

        const transcript = await readClaudeCodeSessionTranscript(projectsDir, 'session-a', {
            includeRawPayloads: false,
        });

        expect(transcript?.rawPayloadsOmitted).toBe(true);
        expect(transcript?.rawEvents).toEqual([]);
        expect(transcript?.entries).toHaveLength(3);
        expect(transcript?.entries[0]?.raw).toEqual({});
        expect(transcript?.entries[1]?.parts[0]?.raw).toEqual({});
        expect(transcript?.entries[1]?.parts[0]).toMatchObject({
            text: 'Need inspect the vendor detection path.',
            type: 'thinking',
        });
        expect(transcript?.session).toMatchObject({
            messageCount: 2,
            sessionId: 'session-a',
            toolCallCount: 1,
        });
    });

    it('should locate and delete a Claude session by its body session id when the filename differs', async () => {
        const projectsDir = await makeTempRoot();
        const sessionId = 'canonical-session-id';
        await writeSession(
            projectsDir,
            '-Users-rhaq-workspace-ushman-corpus',
            'stale-storage-name',
            buildSessionRecords(sessionId, corpusCwd),
        );

        const transcript = await readClaudeCodeSessionTranscript(projectsDir, sessionId);
        const deleted = await deleteClaudeCodeSession(projectsDir, sessionId);

        expect(transcript?.session.sessionId).toBe(sessionId);
        expect(deleted.deletedSessionIds).toEqual([sessionId]);
        expect(deleted.deletedFiles).toHaveLength(1);
        expect(await Bun.file(deleted.deletedFiles[0]!).exists()).toBe(false);
    });

    it('should preserve assistant phases when raw payloads are omitted', async () => {
        const projectsDir = await makeTempRoot();
        await writeSession(projectsDir, '-Users-rhaq-workspace-ushman-corpus', 'session-phases', [
            {
                cwd: corpusCwd,
                message: { content: 'Do the work', role: 'user' },
                sessionId: 'session-phases',
                timestamp: '2026-06-01T10:00:00.000Z',
                type: 'user',
                uuid: 'user-phases',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: "I'll start by reading the required docs and fetching the issues in parallel.",
                    role: 'assistant',
                    stop_reason: 'tool_use',
                },
                sessionId: 'session-phases',
                timestamp: '2026-06-01T10:00:01.000Z',
                type: 'assistant',
                uuid: 'assistant-commentary',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: "All tasks complete. Here's the summary...",
                    role: 'assistant',
                    stop_reason: 'end_turn',
                },
                sessionId: 'session-phases',
                timestamp: '2026-06-01T10:00:02.000Z',
                type: 'assistant',
                uuid: 'assistant-final',
            },
        ]);

        const transcript = await readClaudeCodeSessionTranscript(projectsDir, 'session-phases', {
            includeRawPayloads: false,
        });
        const assistantPhases = transcript?.entries
            .filter((entry) => entry.role === 'assistant')
            .map(getClaudeCodeAssistantMessagePhase);

        expect(transcript?.rawPayloadsOmitted).toBe(true);
        expect(assistantPhases).toEqual(['commentary', 'final_answer']);
    });

    it('should hide API errors and classify their parent assistant response as commentary', async () => {
        const projectsDir = await makeTempRoot();
        await writeSession(projectsDir, '-Users-rhaq-workspace-ushman-corpus', 'session-api-error', [
            {
                cwd: corpusCwd,
                message: { content: 'Continue', role: 'user' },
                sessionId: 'session-api-error',
                timestamp: '2026-06-01T10:00:00.000Z',
                type: 'user',
                uuid: 'user-api-error',
            },
            {
                cwd: corpusCwd,
                message: {
                    content:
                        'Understood - leaving 7.0.2 as-is. Let me record the result and confirm the change is complete.',
                    role: 'assistant',
                    stop_reason: 'end_turn',
                },
                parentUuid: 'user-api-error',
                sessionId: 'session-api-error',
                timestamp: '2026-06-01T10:00:01.000Z',
                type: 'assistant',
                uuid: 'assistant-interrupted',
            },
            {
                cwd: corpusCwd,
                error: 'server_error',
                isApiErrorMessage: 'true',
                message: {
                    content: [
                        {
                            text: 'API Error: Connection closed mid-response. The response above may be incomplete.',
                            type: 'text',
                        },
                    ],
                    model: '<synthetic>',
                    role: 'assistant',
                    stop_reason: 'stop_sequence',
                },
                parentUuid: 'assistant-interrupted',
                sessionId: 'session-api-error',
                timestamp: '2026-06-01T10:00:02.000Z',
                type: 'assistant',
                uuid: 'assistant-api-error',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: "All tasks complete. Here's the summary...",
                    role: 'assistant',
                    stop_reason: 'end_turn',
                },
                sessionId: 'session-api-error',
                timestamp: '2026-06-01T10:00:03.000Z',
                type: 'assistant',
                uuid: 'assistant-complete',
            },
        ]);

        const transcript = await readClaudeCodeSessionTranscript(projectsDir, 'session-api-error', {
            includeRawPayloads: false,
        });
        const assistantMessages = transcript?.entries
            .filter((entry) => entry.role === 'assistant')
            .map((entry) => ({
                phase: getClaudeCodeAssistantMessagePhase(entry),
                text: entry.parts.find((part) => part.type === 'text')?.text,
            }));

        expect(assistantMessages).toEqual([
            {
                phase: 'commentary',
                text: 'Understood - leaving 7.0.2 as-is. Let me record the result and confirm the change is complete.',
            },
            { phase: 'final_answer', text: "All tasks complete. Here's the summary..." },
        ]);
        expect(transcript?.session.assistantMessageCount).toBe(2);
    });

    it('should classify assistant responses followed by subagent task notifications as commentary', async () => {
        const projectsDir = await makeTempRoot();
        await writeSession(projectsDir, '-Users-rhaq-workspace-ushman-corpus', 'session-subagents', [
            {
                cwd: corpusCwd,
                message: { content: 'Run the review with finder agents', role: 'user' },
                sessionId: 'session-subagents',
                timestamp: '2026-06-01T10:00:00.000Z',
                type: 'user',
                uuid: 'user-subagents',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: 'Waiting on the last three finder agents.',
                    role: 'assistant',
                    stop_reason: 'end_turn',
                },
                parentUuid: 'user-subagents',
                sessionId: 'session-subagents',
                timestamp: '2026-06-01T10:00:01.000Z',
                type: 'assistant',
                uuid: 'assistant-waiting',
            },
            {
                content:
                    '<task-notification>\n<task-id>finder-1</task-id>\n<status>completed</status>\n</task-notification>',
                operation: 'enqueue',
                sessionId: 'session-subagents',
                timestamp: '2026-06-01T10:00:02.000Z',
                type: 'queue-operation',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: 'The review is complete. Here are the findings.',
                    role: 'assistant',
                    stop_reason: 'end_turn',
                },
                sessionId: 'session-subagents',
                timestamp: '2026-06-01T10:00:03.000Z',
                type: 'assistant',
                uuid: 'assistant-complete',
            },
        ]);

        const transcript = await readClaudeCodeSessionTranscript(projectsDir, 'session-subagents', {
            includeRawPayloads: false,
        });
        const assistantMessages = transcript?.entries
            .filter((entry) => entry.role === 'assistant')
            .map((entry) => ({
                phase: getClaudeCodeAssistantMessagePhase(entry),
                text: entry.parts.find((part) => part.type === 'text')?.text,
            }));

        expect(assistantMessages).toEqual([
            { phase: 'commentary', text: 'Waiting on the last three finder agents.' },
            { phase: 'final_answer', text: 'The review is complete. Here are the findings.' },
        ]);
    });

    it('should classify status responses followed by user-message task notifications as commentary', async () => {
        const projectsDir = await makeTempRoot();
        const waitingMessage =
            "Waiting on two signals: my fast-tier baseline finishing, and the other session's `test:all` clearing out so I can run an uncontended validation of the new parallel pipeline.";
        await writeSession(projectsDir, '-Users-rhaq-workspace-ushman-corpus', 'session-background-task', [
            {
                cwd: corpusCwd,
                message: { content: 'Optimize the test pipeline', role: 'user' },
                sessionId: 'session-background-task',
                timestamp: '2026-06-01T10:00:00.000Z',
                type: 'user',
                uuid: 'user-request',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: [{ thinking: "I'm waiting for notifications before wrapping up.", type: 'thinking' }],
                    role: 'assistant',
                    stop_reason: 'end_turn',
                },
                parentUuid: 'user-request',
                sessionId: 'session-background-task',
                timestamp: '2026-06-01T10:00:01.000Z',
                type: 'assistant',
                uuid: 'assistant-reasoning',
            },
            {
                cwd: corpusCwd,
                message: { content: waitingMessage, role: 'assistant', stop_reason: 'end_turn' },
                parentUuid: 'assistant-reasoning',
                sessionId: 'session-background-task',
                timestamp: '2026-06-01T10:00:02.000Z',
                type: 'assistant',
                uuid: 'assistant-waiting',
            },
            {
                cwd: corpusCwd,
                message: { content: [], role: 'system' },
                parentUuid: 'assistant-waiting',
                sessionId: 'session-background-task',
                subtype: 'stop_hook_summary',
                timestamp: '2026-06-01T10:00:03.000Z',
                type: 'system',
                uuid: 'stop-hook',
            },
            {
                cwd: corpusCwd,
                message: {
                    content:
                        '<task-notification>\n<task-id>fast-tier</task-id>\n<status>completed</status>\n</task-notification>',
                    role: 'user',
                },
                parentUuid: 'stop-hook',
                sessionId: 'session-background-task',
                timestamp: '2026-06-01T10:00:04.000Z',
                type: 'user',
                uuid: 'task-notification',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: 'The parallel pipeline is validated. Here are the results.',
                    role: 'assistant',
                    stop_reason: 'end_turn',
                },
                parentUuid: 'task-notification',
                sessionId: 'session-background-task',
                timestamp: '2026-06-01T10:00:05.000Z',
                type: 'assistant',
                uuid: 'assistant-complete',
            },
        ]);

        const transcript = await readClaudeCodeSessionTranscript(projectsDir, 'session-background-task', {
            includeRawPayloads: false,
        });
        const assistantMessages = transcript?.entries
            .filter((entry) => entry.role === 'assistant' && entry.parts.some((part) => part.type === 'text'))
            .map((entry) => ({
                phase: getClaudeCodeAssistantMessagePhase(entry),
                text: entry.parts.find((part) => part.type === 'text')?.text,
            }));
        const finalOnlyExport = transcript
            ? renderClaudeCodeTranscript(transcript, {
                  includeCommentary: false,
                  includeMetadata: false,
                  includeTools: false,
                  outputFormat: 'md',
              })
            : null;

        expect(assistantMessages).toEqual([
            { phase: 'commentary', text: waitingMessage },
            { phase: 'final_answer', text: 'The parallel pipeline is validated. Here are the results.' },
        ]);
        expect(finalOnlyExport).not.toContain(waitingMessage);
        expect(finalOnlyExport).toContain('The parallel pipeline is validated. Here are the results.');
    });

    it('should classify an assistant response interrupted by the user as commentary', async () => {
        const projectsDir = await makeTempRoot();
        await writeSession(projectsDir, '-Users-rhaq-workspace-ushman-corpus', 'session-interrupted', [
            {
                cwd: corpusCwd,
                message: { content: 'Prototype the mechanics', role: 'user' },
                sessionId: 'session-interrupted',
                timestamp: '2026-06-01T10:00:00.000Z',
                type: 'user',
                uuid: 'user-request',
            },
            {
                cwd: corpusCwd,
                message: {
                    content: 'Now let me prototype the key mechanics in scratch before writing the suite.',
                    id: 'message-interrupted',
                    model: 'claude-opus-4-8',
                    role: 'assistant',
                    stop_reason: null,
                },
                parentUuid: 'user-request',
                sessionId: 'session-interrupted',
                timestamp: '2026-06-01T10:00:01.000Z',
                type: 'assistant',
                uuid: 'assistant-interrupted',
            },
            {
                cwd: corpusCwd,
                message: { content: '[Request interrupted by user]', role: 'user' },
                parentUuid: 'assistant-interrupted',
                sessionId: 'session-interrupted',
                timestamp: '2026-06-01T10:00:02.000Z',
                type: 'user',
                uuid: 'user-interrupted',
            },
            {
                cwd: corpusCwd,
                message: { content: 'Implementation complete.', role: 'assistant', stop_reason: 'end_turn' },
                sessionId: 'session-interrupted',
                timestamp: '2026-06-01T10:00:03.000Z',
                type: 'assistant',
                uuid: 'assistant-complete',
            },
        ]);

        const transcript = await readClaudeCodeSessionTranscript(projectsDir, 'session-interrupted', {
            includeRawPayloads: false,
        });
        const assistantPhases = transcript?.entries
            .filter((entry) => entry.role === 'assistant')
            .map(getClaudeCodeAssistantMessagePhase);

        expect(assistantPhases).toEqual(['commentary', 'final_answer']);
        expect(transcript?.entries.some((entry) => entry.entryId === 'user-interrupted')).toBe(false);
        const rendered = transcript
            ? renderClaudeCodeTranscript(transcript, {
                  includeCommentary: false,
                  includeMetadata: false,
                  includeTools: false,
                  outputFormat: 'md',
              })
            : null;
        expect(rendered).not.toContain('Now let me prototype the key mechanics');
        expect(rendered).toContain('Implementation complete.');
    });

    it('should delete a Claude Code session JSONL file', async () => {
        const projectsDir = await makeTempRoot();
        const sessionPath = path.join(projectsDir, '-Users-rhaq-workspace-ushman-corpus', 'session-delete.jsonl');
        await writeSession(
            projectsDir,
            '-Users-rhaq-workspace-ushman-corpus',
            'session-delete',
            buildSessionRecords('session-delete', corpusCwd),
        );

        const result = await deleteClaudeCodeSession(projectsDir, 'session-delete');

        expect(result.deletedSessionIds).toEqual(['session-delete']);
        expect(result.deletedFiles).toEqual([sessionPath]);
        expect(await Bun.file(sessionPath).exists()).toBe(false);
        expect(await readClaudeCodeSessionTranscript(projectsDir, 'session-delete')).toBeNull();
        expect(
            await listClaudeCodeSessionsForGroup('project:-Users-rhaq-workspace-ushman-corpus', projectsDir),
        ).toEqual([]);
    });

    it('should parse numeric Claude Code timestamps as epoch milliseconds', async () => {
        const projectsDir = await makeTempRoot();
        await writeSession(projectsDir, '-Users-rhaq-workspace-ushman-corpus', 'session-numeric', [
            {
                cwd: corpusCwd,
                message: {
                    content: 'Numeric timestamp fixture',
                    role: 'user',
                },
                sessionId: 'session-numeric',
                timestamp: 1_780_308_000_000,
                type: 'user',
                uuid: 'session-numeric-user-1',
            },
        ]);

        const transcript = await readClaudeCodeSessionTranscript(projectsDir, 'session-numeric');

        expect(transcript?.session.createdAtMs).toBe(1_780_308_000_000);
        expect(transcript?.session.createdAtIso).toBe('2026-06-01T10:00:00.000Z');
    });

    it('should return empty results when Claude Code data is missing', async () => {
        const missingProjectsDir = path.join(os.tmpdir(), `spiracha-missing-claude-code-projects-${randomUUID()}`);

        expect(await listClaudeCodeWorkspaceGroups(missingProjectsDir)).toEqual([]);
        expect(await listClaudeCodeSessionsForGroup('project:missing', missingProjectsDir)).toEqual([]);
        expect(await readClaudeCodeSessionTranscript(missingProjectsDir, 'missing')).toBeNull();
    });
});
